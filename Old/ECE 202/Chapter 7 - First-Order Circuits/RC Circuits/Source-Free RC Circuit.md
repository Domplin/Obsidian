The objective is to determine the [[Circuit Response]], which is assumed to be the voltage $v(t)$ across the capacitor.

Since the capacitor is initially charged at $t=0$ the initial voltage is:
$$
v(0)=V_{0}
$$

$v(0)$ is needed to calculate [[Energy Stored in Capacitor]]

To find the current $i_{R}(t)$ using the equation from the [[Capacitive Time Constant]] 